# Provider Adapters

Provider adapters translate durable `ai_generation_jobs` records into provider
requests and return normalized image bytes plus metadata.

Start with a mocked adapter before enabling OpenAI or xAI calls.
