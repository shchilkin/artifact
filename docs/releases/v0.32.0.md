# v0.32.0 Release Notes

Artifact v0.32.0 is the Code Health and Debt Reduction release. It turns the
post-v0.31 Fallow cleanup into a safer maintenance baseline by classifying
historical complexity hotspots, cleaning React hook warnings, documenting the
CSS/Tailwind boundary, and preparing the next debt work without changing editor
semantics.

This release changes release planning, code-health documentation, package
metadata, and small hook dependency boundaries. It deliberately does not ship a
new product feature or broaden renderer/editor behavior.

Document schema, graph traversal, renderer/export behavior, thumbnail
scheduling, AI scope, package export, and font policy remain unchanged.

## Highlights

- Added the v0.32 code-health plan covering Fallow complexity policy,
  route-level editor orchestration debt, React hook warnings, canvas gesture
  boundaries, CSS/Tailwind strategy, roadmap cleanup, and storage/render risk
  triage.
- Captured the v0.32 Fallow health review in
  `docs/fallow-v0.32-health.md`: health score `88.4 / A`, 75 historical
  functions above threshold, and changed-code audit `verdict: "pass"`.
- Fixed React hook dependency warnings in AI generation, node context menus,
  node selection sync, thumbnail rendering, and document rendering without
  weakening lint rules.
- Documented why the node editor remains CSS-first while Tailwind stays useful
  for route shells, wrappers, and simple low-state layout.
- Bumped package metadata to `0.32.0` in `package.json`,
  `apps/web/package.json`, and `package-lock.json`.

## Scope Boundaries

- Included: v0.32 package metadata bump.
- Included: Fallow health classification and changed-code audit validation.
- Included: React hook warning cleanup with stable refs/memoized option
  boundaries where needed.
- Included: roadmap, version-plan, production-readiness, and release-notes
  cleanup for the v0.32 release thesis.
- Included: storage/render and CSS/Tailwind risk classification.
- Deferred: full-health complexity gate for historical hotspots.
- Deferred: broad `NodeCanvas`, `randomConfig`, Add Library preview,
  persistence, renderer, API handler, and CSS monolith refactors.
- Deferred: landing work, Showcase / How-to work, command palette,
  server-backed sharing, dynamic OG generation, graph traversal changes, export
  changes, persistence schema changes, AI scope changes, package export
  changes, and font-policy changes.

## Validation

Local validation passed during release prep on 2026-06-06:

- `npm run check`
- `npm run build`
- `FALLOW_AGENT_SOURCE=codex npm run --silent fallow:audit -- --base origin/development`
- `FALLOW_AGENT_SOURCE=codex npx fallow health --format json --quiet --explain --score --complexity --top 30`

Browser validation:

The full browser gate passed with `270 passed, 25 skipped` across Chromium,
Firefox, WebKit, mobile Chromium, and mobile WebKit.

During release prep, an earlier full browser run produced two timeout failures
that both passed when rerun directly. The final full browser gate then passed
cleanly.

Performance notes:

- `npm run perf:node-editor` is required before tagging if the final release
  includes any additional React Flow, node-editor interaction, thumbnail
  scheduling, or CSS behavior changes. The current v0.32 prep changes hook
  dependency boundaries and docs only; no graph traversal, high-frequency
  gesture, or renderer behavior changed.

## Manual QA

- Confirmed PR #74 exists for the v0.32 branch and currently targets
  `development`.
- Confirmed changed-code Fallow audit is clean for the v0.32 branch.
- Confirmed the full browser gate passes after the previously timed-out browser
  scenarios passed when rerun directly.

## Accepted Risks

- Historical complexity remains visible in the full-health report. v0.32
  classifies the hotspots and keeps the changed-code audit blocking, but does
  not turn the historical full-health list into a release blocker.
- Broad route/controller, node-canvas, CSS, persistence, Add Library preview,
  random preset, and API handler refactors are deferred until each has focused
  tests and a narrower release thesis.
- npm install previously reported the existing dependency audit state: 11
  vulnerabilities (4 moderate, 7 high). npm audit remediation is outside the
  v0.32 code-health release scope and is not part of the current release gate.
