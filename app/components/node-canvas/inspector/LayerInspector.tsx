// Re-export from the canonical renderer. Field ranges and option lists live in
// app/components/layer-controls/fieldDefs.ts so sidebar and inspector never drift.
export { LayerControls as LayerInspector } from '../../../components/layer-controls/LayerControls';
