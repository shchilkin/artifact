# v0.28 Editor Guardrails v2

## Summary

v0.28 continues the editor-confidence track after v0.27. The goal is to make
editing feel more predictable in dense real projects: users should know what is
selected, where it lives, whether it affects export, and which actions are
available before they commit a destructive or structural change.

This is a guardrails and orientation slice, not a renderer or backend slice. It
should preserve document/render/export semantics unless a focused bug fix proves
that existing behavior is already inconsistent.

## Goals

- Clarify lock behavior across Layers and Nodes without surprising graph or
  export changes.
- Make selected-target breadcrumbs more useful: layer stack position, folder or
  area membership where available, and graph output-path context.
- Make hidden, locked, selected output target, graph-only utility, source, and
  effect states read consistently in the properties surfaces.
- Keep `LayerControls` shared between Layers and Nodes.
- Add focused tests for the guardrail rules before broad browser coverage.

## Non-Goals

- No new renderer algorithm.
- No graph traversal or export-path redesign.
- No AI, account, project sync, share-link storage, or backend expansion.
- No corporate/admin UI treatment.
- No broad visual redesign of the editor shell.
- No coverage thresholds unless separately approved after reviewing the v0.27
  baseline.

## Implementation Checklist

### 1. Lock Policy Audit

- [ ] Document the current meaning of layer-backed lock in Layers and Nodes.
- [ ] Decide whether graph-only utility nodes need a durable lock field, or
  whether v0.28 should stay with layer-backed lock only.
- [ ] Keep any new durable field out of `CanvasDocument` unless the schema,
  migration, docs, tests, package roundtrip, and import/export behavior are all
  updated together.
- [ ] Add pure tests for delete/reorder/move guardrail policy helpers.

### 2. Breadcrumbs And Target Context

- [ ] Extend the shared editor target summary with compact breadcrumb data.
- [ ] Show layer stack position and folder/area membership where the document
  already has that information.
- [ ] Show selected graph target output-path status without requiring users to
  infer it from edge color alone.
- [ ] Keep the copy short enough for the current low-chrome editor surfaces.

### 3. Properties Panel Parity

- [ ] Make Layers selected controls and Nodes properties panel use the same
  target status vocabulary.
- [ ] Confirm source/effect/utility/output distinction remains clear.
- [ ] Confirm hidden and locked states disable or explain only the actions they
  actually affect.
- [ ] Avoid duplicating effect-control metadata between Layers and Nodes.

### 4. Browser Guardrails

- [ ] Add browser coverage for locked layer reorder/delete.
- [ ] Add browser coverage for locked layer-backed node delete behavior.
- [ ] Add browser coverage for hidden selected target status in Layers and
  Nodes.
- [ ] Add browser coverage for output-path and no-input output target status.

### 5. Docs And Release Prep

- [ ] Update `docs/state-model.md` if lock semantics change.
- [ ] Update `docs/node-editor.md` if node guardrail behavior changes.
- [ ] Update `docs/testing.md` if coverage or browser guardrail strategy
  changes.
- [ ] Keep release notes explicit about any deferred graph-only lock behavior.

## Validation

- [ ] `npm run check`
- [ ] `npm run build`
- [ ] `npm run test:browser`
- [ ] Focused browser coverage for guardrails and target breadcrumbs.
- [ ] `npm run perf:node-editor` only if React Flow wiring, thumbnail
  scheduling, node preview rendering, or high-frequency gestures change.

## Acceptance Criteria

- Users can tell what is selected and whether it affects export in both Layers
  and Nodes.
- Locked and hidden targets have consistent, visible guardrails.
- Breadcrumb/target context helps in dense projects without adding heavy UI.
- Layers and Nodes keep shared control behavior.
- No render/export semantic change is introduced.
- No schema migration is introduced unless the migration and package/import
  behavior are implemented and tested in the same slice.

## Deferred Candidates

- Server-backed share links.
- Account-synced project and font libraries.
- Visual regression snapshots for editor state.
- Graph-only lock schema if v0.28 chooses not to introduce it.
