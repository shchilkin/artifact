# v0.23 Graph Add Library Drag

## Summary

v0.23 should make the node editor feel more direct after v0.22 hardened the
local project and portable asset story.

The first slice is **Graph Add Library Drag v2**: users should be able to drag
items from the shared Add Library onto useful graph insertion targets, not only
drop new nodes onto empty canvas. The goal is faster graph construction without
changing graph traversal, renderer/export semantics, thumbnail scheduling,
document schema, or the shared Add Library catalog model.

This is an interaction-quality release, not a new effect/node-kind release.

## Goals

- Keep the existing click-to-add and drag-to-canvas Add Library behavior.
- Add clear insertion affordances for dragging a library item onto an edge.
- Support replacing the current "drop connection on empty canvas" flow with
  the same shared Add Library insertion model where practical.
- Insert compatible layer/effect/source nodes by using existing graph helper
  boundaries, especially edge splitting helpers.
- Preserve Layers and Nodes parity: the Add Library catalog stays shared, while
  graph insertion behavior remains node-canvas specific.
- Keep graph state serializable and undoable through existing document update
  flow.
- Make the drag feedback readable at normal and zoomed-out graph scales without
  corporate/admin chrome.

## Non-Goals

- No new graph node kind.
- No new `CanvasDocument` schema fields.
- No renderer, export, graph traversal, or thumbnail scheduling changes.
- No command palette.
- No account/backend/project-sync work.
- No large Add Library redesign beyond the insertion affordances needed for
  dragging.
- No Layers-panel drag-to-graph behavior in this slice.

## Implementation Checklist

- [x] Move `v0.23` to the active roadmap slot and mark `v0.22` as shipped.
- [ ] Audit current Add Library drag payloads and node-canvas drop handling.
- [ ] Audit graph helper coverage for edge insertion/splitting and document
  history updates.
- [ ] Add a pure helper for resolving an insertion target from dragged library
  item + graph edge/node context.
- [ ] Add unit coverage for insertion-target compatibility and edge-splitting
  behavior.
- [ ] Add edge hover/drop affordance while dragging Add Library items over the
  node canvas.
- [ ] Add drop-on-edge insertion for compatible layer/effect/source items.
- [ ] Preserve empty-canvas drop behavior and toolbar/right-click Add Library
  placement.
- [ ] Ensure dropped nodes receive stable graph positions near the insertion
  point without shifting unrelated nodes.
- [ ] Ensure undo/redo treats one drag insertion as one document update.
- [ ] Add focused browser coverage for:
  - dragging an effect from Add Library onto empty canvas;
  - dragging an effect onto an existing edge and verifying it splits the edge;
  - output preview remains nonblank after insertion;
  - existing click-to-add behavior still works;
  - Layers -> Nodes -> Layers preview remains nonblank.
- [ ] Update `docs/node-editor.md` if interaction grammar changes.

## Validation

- [ ] `npm run check`
- [ ] `npm run build`
- [ ] `npm run test:browser`
- [ ] `npm run perf:node-editor` if React Flow wiring, drag hover state,
  thumbnail scheduling, or node preview render behavior changes.

Focused validation:

- [ ] Unit tests cover insertion target selection and graph edge split behavior.
- [ ] Focused Chromium browser test covers drag-to-edge insertion.
- [ ] Existing Add Library drag-to-canvas and click-to-add browser coverage
  remains green.
- [ ] Full browser suite passes before release prep.

## Acceptance Criteria

- Users can drag a compatible Add Library item onto an existing edge and get a
  correctly inserted node.
- Empty-canvas drop, click-to-add, and right-click add menu behavior remain
  intact.
- Inserted graph edges are valid, undoable, and cycle-safe.
- Output preview and export-target rendering stay nonblank after insertion.
- No document schema migration is required.
- Graph traversal, renderer/export output, thumbnail scheduling, and layer
  stack semantics are unchanged.
- Browser behavior remains stable across Chromium, Firefox, WebKit, and mobile
  smoke tests.

## Future Candidates

- Drag Add Library items onto specific node handles when compatible.
- Multi-node recipe drag from Add Library.
- Command palette entry point for Add Library actions.
- Server-backed share links and `.artifact` project bundles for large imported
  payloads.
- Font licensing policy for portable projects: raster artwork export stays
  pixel-only, while bundled font files require allowlists, license notices, or
  explicit user confirmation for user-provided fonts.
- Typography Library v3 with external Google Fonts catalog search/import.
